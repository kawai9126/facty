<h2>会員一覧</h2>
<div>
    <table class="table_en_in">
        <tr>
            <th>会員ID</th>
            <th>ニックネーム</th>
            <th>氏名</th>
            <th>メールアドレス</th>
        </tr>
        <tr>
        <% @end_users.each do |end_user| %>
        <tr>
            <td><%= end_user.id %></td>
            <td><%= end_user.nick_name %></td> 
            <td><%= link_to end_user.user_name, admin_end_user_path(end_user) %></td>
            <td><%= end_user.email %></td>
      　<td>
        <% end %>
        <%= paginate @end_users %>
        </tr>
    </table>
</div>
